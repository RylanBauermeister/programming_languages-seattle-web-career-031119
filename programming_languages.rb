def reformat_languages(languages)
  # your code here
  result = {}
  languages.each do |style, langs|
    language.each do |lang, types|
      result[lang] = {
        type: types.values()
      }
    end
  end
  
end
